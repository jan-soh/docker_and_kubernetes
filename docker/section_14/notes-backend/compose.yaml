name: notes-backend

services:
  notes:
    build:
      context: .
      # This will execute only the development stage from the Dockerfile.
      target: development
    ports:
      - 3002:80
    environment:
      - PORT=80
      # We can use interpolation here. In this case Docker will replace ${NOTEBOOKS_DB_USER} with the value of the NOTEBOOKS_DB_USER environment variable.
      # To be able to find that variable, it must be defined in the .env file.
      - DB_URL=mongodb://${NOTES_DB_USER}:${NOTES_DB_PASSWORD}@notes-db:27017/${NOTES_DB_NAME}
    develop:
      watch:
        - action: sync
          path: ./src
          target: /app/src
    networks:
      - notes-net
    depends_on:
      - notes-db

  notes-db:
    image: mongodb/mongodb-community-server:7.0-ubuntu2204
    env_file:
      - .env
    volumes:
      - type: volume
        source: notes-data
        target: /data/db
      - type: bind
        source: ./db-config/mongo-init.js
        target: /docker-entrypoint-initdb.d/mongo-init.js
        read_only: true
    networks:
      - notes-net


volumes:
  notes-data:

networks:
  notes-net:
  proxy-net: